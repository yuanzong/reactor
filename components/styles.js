'use strict';

var { StyleSheet } = require('react-native');

module.exports = StyleSheet.create({
  appContainer: {
    backgroundColor: 'white'
  },

  offerContainer: {
    margin: 2,
    borderRadius: 3,
    borderWidth: 1,
    borderColor: 'black',
    height: 120
  }
});
